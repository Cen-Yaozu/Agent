---
"@agentxjs/portagent": patch
---

refactor(portagent): simplify environment variable names

- Rename `PORTAGENT_DATA_DIR` to `DATA_DIR` for brevity
- Remove deprecated `PORTAGENT_PASSWORD` environment variable and CLI option
